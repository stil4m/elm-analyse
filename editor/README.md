# Dev

Run from the root

```
npm link                    # Links the elm-analyse to use the cloned version instead of the globally installed.
npm install                 # Install deps
npm run build               # Build the normal stuff

npm run dev:build-editor    # Build the editor makes the JS file available at http://localhost:3000/editor-elm.js
```
